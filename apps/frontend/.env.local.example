# Frontend environment variables (example)
# Copy to .env.local on your machine or set in Vercel project settings

# Public API base URL of your backend (Railway/Render)
NEXT_PUBLIC_API_BASE=https://your-backend.example.com
